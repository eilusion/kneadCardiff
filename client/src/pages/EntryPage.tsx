import React from "react";
// import "./EntryPage.css";

type TEntryPageProps = {}

export const EntryPage = (props: React.PropsWithChildren<TEntryPageProps>): JSX.Element => {
    return (
        <div className="entry-page">
            hello
        </div>
    );
};